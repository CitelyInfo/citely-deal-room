{
  "name": "citely-deal-room-webmcp-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build --outDir .invite-build && node scripts/build-invite-server.mjs",
    "build:static": "vite build",
    "preview": "vite preview",
    "test": "vitest run && node --test tests/invite-gate.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jsdom": "^30.0.0",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "vitest": "^3.2.4"
  }
}
