# pipeline/ — the client's repository (demo prop)

In the demo this directory plays the role of Northstar's capture-pipeline repo on GitHub.
An agent asked to "read our pipeline repo" should find `lineage.py` and propose that material **m1 (data lineage records)** is `pending` with evidence pointing here — it must not mark it `provided`; only the client can confirm that.
Synthetic code; not part of the web app build.
